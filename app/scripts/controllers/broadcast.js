'use strict';

angular.module('partybotWebApp')
  .controller('BroadcastCtrl', function ($scope, $http) {
    $http.get('/api/awesomeThings').success(function(awesomeThings) {
      $scope.awesomeThings = awesomeThings;
    });
  });
